# Results

This directory contains curated, compact evidence for production agents.

There is one accepted agent:

- [`accepted-r1/`](accepted-r1/README.md): ExIt r1 on `gen9randombattle`.

Smoke tests, failed gates, partial gates, replay logs, and research metrics are
not production results. They are preserved under `experimental/runs/`.
