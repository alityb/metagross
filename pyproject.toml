[project]
name = "metagross"
version = "0.1.0"
description = "Superhuman Gen1-9 random battle agent via neural MCTS + recursive language model"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
